require "spec_helper"

describe QueryHelpers do
  # let(:db) { Database.new }

  # describe ".connect" do
  #   it "returns an sqlite database connection" do
  #     expect(database).to be_instance_of(SQLite3::Database)
  #   end
  # end 
end
